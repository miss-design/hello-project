spring:
  cloud:
    config:
      name: config-client
      label: master
      discovery:
        enabled: true
        service-id: configserver
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/